services:
  db:
    image: postgres:16
    container_name: bid-scheduler-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-bid_scheduler}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-bid_scheduler_dev}
      POSTGRES_DB: ${POSTGRES_DB:-bid_scheduler}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-bid_scheduler}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  db_data:
